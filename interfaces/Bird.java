package interfaces;

// modifier => pulic default

// extends multiple interfaces inside interface using , comma
// using interface, we can acheive full abstraction means, we can define WHAT class must do but not HOW it will do;
// can not create object of interface but only can create object concreate class which implemented interface
// multiple inheritence
// abstraction
// hold refrence of an object and used while compile
// multiple inheritance
// interface can have signature only

public interface Bird {
    public void fly(); //=> BODY defined only method
}
